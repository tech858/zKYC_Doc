---
title: Aptos Integration
description: Sign your first Aptos commitment in under  10 minutes.
---

This guide walks you through on how to use zKYC to allows user to verify their identity as well as signing a commit ment in web3.

## Prerequisites

- Node.js 18 or higher
- A zKYC account — [register at app.zkyc.tech](https://app.zkyc.tech)
- An API key from your dashboard (test key is fine to start)

---

## Step 1 — Install the SDK
```bash
npm i zkyc-aptos-package@latest
```

---

## Step 2 — Trigger Verification

Call `ZKYCProcess` when a user needs to verify their identity.
This redirects them to the hosted zKYC verification flow.
```tsx
import { ZKYCProcess } from "zkyc-sdk-package";

export default function VerifyButton() {
  const handleVerify = async () => {
    try {
      await ZKYCProcess({
        apiKey: "test_your_key_here",
        successPage: `${window.location.origin}/verification-success`,
        failurePage: `${window.location.origin}/verification-failed`,
      });
    } catch (error) {
      console.error("Failed to start verification:", error);
    }
  };

  return <button onClick={handleVerify}>Verify Identity</button>;
}
```

<Callout type="info">
  Use a `test_` prefixed key during development. Test keys use a sandboxed environment with simulated documents and do not charge per verification.
  Switch to a `prod_` key when going live. Production keys require a $100 minimum deposit.
</Callout>











## What Happens Next

The user completed KYC. Their proof is:
- Issued to them as a code string they will recieve at the end of the kyc process
- Optionally anchored on Aptos if they connected a wallet
- Valid until their underlying document expires

If you want to understand the full lifecycle, read the [Flow Architecture](/docs/flow-architecture/01-macro-architecture).

If you want to set up webhooks instead of polling, see [Webhooks](/docs/reference/03-webhooks).

